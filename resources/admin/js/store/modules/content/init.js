import index from './index'
import editor from './editor'
import templates from './templates'

export default {
    namespaced: true,
    modules: {
        index,
        editor,
        templates
    }
}
