import Vue from 'vue'
import components from './index.js'

// _.forEach(components, (o, nameKey) => {
// 	o.block = Vue.extend({
// 		extends: o.block
// 	})
// 	o.settings = Vue.extend({
// 		extends: o.settings
// 	})
// })

export default components